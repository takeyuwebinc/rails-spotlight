<div class="sm:px-8 mt-16 sm:mt-32">
  <div class="mx-auto w-full max-w-7xl lg:px-8">
    <div class="relative px-4 sm:px-8 lg:px-12">
      <div class="mx-auto max-w-2xl lg:max-w-5xl">
        <header class="max-w-2xl">
          <h1 class="text-4xl font-bold tracking-tight text-zinc-800 dark:text-zinc-100 sm:text-5xl">
            Speaking Engagements
          </h1>
          <p class="mt-6 text-base text-zinc-600 dark:text-zinc-400">
            技術カンファレンスやミートアップでの登壇実績をご紹介します。
          </p>
        </header>
        
        <div class="mt-16 sm:mt-20">
          <ul role="list" class="space-y-16">
            <% @speaking_engagements.each do |engagement| %>
              <li class="group relative flex flex-col items-start">
                <h2 class="mt-2 text-base font-semibold text-zinc-800 dark:text-zinc-100">
                  <div class="absolute -inset-x-4 -inset-y-6 z-0 scale-95 bg-zinc-50 opacity-0 transition group-hover:scale-100 group-hover:opacity-100 sm:-inset-x-6 sm:rounded-2xl dark:bg-zinc-800/50"></div>
                  <% if engagement.event_url.present? %>
                    <a href="<%= engagement.event_url %>" target="_blank" rel="noopener noreferrer" class="relative z-10">
                      <%= engagement.event_name %>
                    </a>
                  <% else %>
                    <span class="relative z-10"><%= engagement.event_name %></span>
                  <% end %>
                </h2>
                <div class="relative z-10 order-first z-10 flex items-center text-sm text-zinc-400 dark:text-zinc-500">
                  <time datetime="<%= engagement.event_date.iso8601 %>" class="flex items-center">
                    <i class="fa-regular fa-calendar mr-1"></i>
                    <span><%= engagement.event_date.strftime("%Y年%-m月%-d日") %></span>
                  </time>
                  <% if engagement.location.present? %>
                    <span class="mx-2">•</span>
                    <span class="flex items-center">
                      <i class="fa-solid fa-location-dot mr-1"></i>
                      <span><%= engagement.location %></span>
                    </span>
                  <% end %>
                </div>
                <% if engagement.description.present? %>
                  <div class="relative z-10 mt-2 text-sm text-zinc-600 dark:text-zinc-400">
                    <%= engagement.description.html_safe %>
                  </div>
                <% end %>
                <% if engagement.tags.any? %>
                  <div class="relative z-10 mt-4 flex flex-wrap gap-2">
                    <% engagement.tags.each do |tag| %>
                      <%= render(BadgeComponent.new(text: tag.name, bg_color: tag.badge_colors[:bg_color], text_color: tag.badge_colors[:text_color])) %>
                    <% end %>
                  </div>
                <% end %>
                <div class="relative z-10 mt-4 flex flex-wrap gap-4">
                  <% if engagement.event_url.present? %>
                    <div class="flex items-center text-sm font-medium text-zinc-400 transition group-hover:text-teal-500 dark:text-zinc-200 dark:group-hover:text-teal-400">
                      <a href="<%= engagement.event_url %>" target="_blank" rel="noopener noreferrer" class="flex items-center">
                        <span>イベントページを見る</span>
                        <i class="fa-solid fa-arrow-up-right-from-square ml-1"></i>
                      </a>
                    </div>
                  <% end %>
                  <% if engagement.slides_url.present? %>
                    <div class="flex items-center text-sm font-medium text-zinc-400 transition group-hover:text-teal-500 dark:text-zinc-200 dark:group-hover:text-teal-400">
                      <a href="<%= engagement.slides_url %>" target="_blank" rel="noopener noreferrer" class="flex items-center">
                        <span>発表資料を見る</span>
                        <i class="fa-solid fa-file-powerpoint ml-1"></i>
                      </a>
                    </div>
                  <% end %>
                </div>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
