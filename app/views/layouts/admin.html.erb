<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "工数管理" %> - 管理画面</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  </head>

  <body class="h-full bg-zinc-100">
    <div class="min-h-full">
      <nav class="bg-zinc-800">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex h-16 items-center justify-between">
            <div class="flex items-center">
              <div class="shrink-0">
                <%= link_to admin_root_path, class: "text-white font-bold text-lg hover:text-zinc-200" do %>
                  工数管理
                <% end %>
              </div>
              <div class="ml-10 flex items-baseline space-x-4">
                <%= link_to "ダッシュボード", admin_root_path,
                    class: "rounded-md px-3 py-2 text-sm font-medium #{request.path == '/admin' ? 'bg-zinc-900 text-white' : 'text-zinc-300 hover:bg-zinc-700 hover:text-white'}" %>
                <%= link_to "工数カレンダー", admin_work_hour_work_entries_path,
                    class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.include?('work_entries') ? 'bg-zinc-900 text-white' : 'text-zinc-300 hover:bg-zinc-700 hover:text-white'}" %>
                <%= link_to "案件", admin_work_hour_projects_path,
                    class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.include?('projects') ? 'bg-zinc-900 text-white' : 'text-zinc-300 hover:bg-zinc-700 hover:text-white'}" %>
                <%= link_to "クライアント", admin_work_hour_clients_path,
                    class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.include?('clients') ? 'bg-zinc-900 text-white' : 'text-zinc-300 hover:bg-zinc-700 hover:text-white'}" %>
                <%= link_to "CSV", admin_work_hour_csv_index_path,
                    class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.include?('csv') ? 'bg-zinc-900 text-white' : 'text-zinc-300 hover:bg-zinc-700 hover:text-white'}" %>
              </div>
            </div>
            <div>
              <%= link_to "サイトへ戻る", root_path, class: "text-zinc-300 hover:text-white text-sm" %>
            </div>
          </div>
        </div>
      </nav>

      <main>
        <div class="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
          <% if notice %>
            <div class="mb-4 rounded-md bg-green-50 p-4">
              <div class="flex">
                <div class="shrink-0">
                  <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd" />
                  </svg>
                </div>
                <div class="ml-3">
                  <p class="text-sm font-medium text-green-800"><%= notice %></p>
                </div>
              </div>
            </div>
          <% end %>

          <% if alert %>
            <div class="mb-4 rounded-md bg-red-50 p-4">
              <div class="flex">
                <div class="shrink-0">
                  <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.28 7.22a.75.75 0 00-1.06 1.06L8.94 10l-1.72 1.72a.75.75 0 101.06 1.06L10 11.06l1.72 1.72a.75.75 0 101.06-1.06L11.06 10l1.72-1.72a.75.75 0 00-1.06-1.06L10 8.94 8.28 7.22z" clip-rule="evenodd" />
                  </svg>
                </div>
                <div class="ml-3">
                  <p class="text-sm font-medium text-red-800"><%= alert %></p>
                </div>
              </div>
            </div>
          <% end %>

          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>
