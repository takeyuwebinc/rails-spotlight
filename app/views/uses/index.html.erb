<div class="sm:px-8 mt-16 sm:mt-32">
  <div class="mx-auto w-full max-w-7xl lg:px-8">
    <div class="relative px-4 sm:px-8 lg:px-12">
      <div class="mx-auto max-w-2xl lg:max-w-5xl">
        <header class="max-w-2xl">
          <h1 class="text-4xl font-bold tracking-tight text-zinc-800 dark:text-zinc-100 sm:text-5xl">
            Tools and Gadgets I Use
          </h1>
          <p class="mt-6 text-base text-zinc-600 dark:text-zinc-400">
            ソフトウェア開発、生産性向上、日常生活で使用しているツールやガジェットをご紹介します。これらは私の作業効率を高め、クリエイティブな仕事をサポートしてくれています。
          </p>
        </header>
        
        <div class="mt-16 sm:mt-20">
          <div class="space-y-20">
            <% @items_by_category.each do |category, items| %>
              <section aria-labelledby="<%= category %>-title" class="md:border-l md:border-zinc-100 md:pl-6 md:dark:border-zinc-700/40">
                <div class="grid max-w-3xl grid-cols-1 items-baseline gap-y-8 md:grid-cols-4">
                  <h2 id="<%= category %>-title" class="text-sm font-semibold text-zinc-800 dark:text-zinc-100">
                    <%= category.humanize %>
                  </h2>
                  <div class="md:col-span-3">
                    <ul role="list" class="space-y-16">
                      <% items.each do |item| %>
                        <li class="group relative flex flex-col items-start">
                          <h3 class="text-base font-semibold tracking-tight text-zinc-800 dark:text-zinc-100">
                            <% if item.url.present? %>
                              <a href="<%= item.url %>" target="_blank" rel="noopener noreferrer" class="hover:text-teal-500 dark:hover:text-teal-400">
                                <%= item.name %>
                              </a>
                            <% else %>
                              <%= item.name %>
                            <% end %>
                          </h3>
                          <div class="relative z-10 mt-2 text-sm text-zinc-600 dark:text-zinc-400">
                            <%= raw item.description %>
                          </div>
                        </li>
                      <% end %>
                    </ul>
                  </div>
                </div>
              </section>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
